import { FormBuilder, FormGroup } from '@angular/forms';
import { Schema } from '../schema';
export declare const Schema2NgForms: <T>(schema: Schema.Table, formBuilder: FormBuilder, doc?: T) => FormGroup;
